{
  "framework": "vite",
  "installCommand": "npm install --include=dev --engine-strict=false",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
